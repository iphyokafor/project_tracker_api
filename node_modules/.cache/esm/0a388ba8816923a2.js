let express,graphqlHTTP;_816‍.w("express",[["default",["express"],function(v){express=v}]]);_816‍.w("express-graphql",[["default",["graphqlHTTP"],function(v){graphqlHTTP=v}]]);



const app = express();

app.use('/graphql', graphqlHTTP({
    schema,
    graphiql: true
}));

const port = process.env.PORT || 8000;

app.listen(port, () => {
    console.log(`Server running on port ${port}`);
});